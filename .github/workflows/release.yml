name: Release
on:
  push:
    branches:
      - master
      - next

permissions:
  id-token: write
  contents: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - uses: voidzero-dev/setup-vp@v1.21.1
        with:
          cache: true
          run-install: false

      - run: vp install --frozen-lockfile
      - run: vp check
      - run: vp exec playwright install --with-deps chromium
      - run: vp test
      - run: vp pack

      - name: Setup git user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email ""

      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: vp env exec pnpm nx release -y
